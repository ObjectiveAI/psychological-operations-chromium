# Python files
__pycache__/
*.py[cod]

# Python testing files
.coverage

# Ignore macOS Finder meta
.DS_Store
.tm_properties

# Ignore optional build / cache directory
/build

# build.sh per-target work + final .zip (intermediate cache; final
# artifacts live on GitHub Releases, not in the repo)
/.build

# run.sh per-session state (--config-base-dir default)
/.run
